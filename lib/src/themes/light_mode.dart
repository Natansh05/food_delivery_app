import 'package:flutter/material.dart';

ThemeData lightMode = ThemeData(
  colorScheme: ColorScheme.light(
    background: Color(0xFF1976D2), // Primary Color (Blue)
    primary: Color(0xFF607D8B), // Secondary Color (Blue Grey)
    secondary: Color(0xFFFFA000), // Tertiary Color (Amber)
    onPrimary: Color(0xFFFFFFFF), // Inverse Primary Color (White)
  ),
);
